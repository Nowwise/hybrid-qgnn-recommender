# Default: CPU PyTorch. For NVIDIA GPU: pip install -r requirements-base.txt && pip install -r requirements-gpu-torch.txt (or scripts/install_gpu.ps1)
# Single flat file (no -r includes): Vercel and other strict parsers reject `-r` in requirements.txt.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy>=1.21.0
tqdm>=4.62.0
scikit-learn>=1.0.0
pennylane>=0.30.0
scipy>=1.7.0
matplotlib>=3.5.0
pandas>=1.3.0
ipython>=8.0.0
jupyter>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0
torch>=2.0.0
